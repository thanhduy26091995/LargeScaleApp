name: feature_orders
description: Orders feature module for LargeScaleApp Flutter
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  contracts:
    path: ../contracts
  wire:
    path: ../wire
  core:
    path: ../core
  shared_ui:
    path: ../shared_ui
  get_it: ^7.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
